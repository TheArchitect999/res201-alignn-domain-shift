# `reports/week2_prof_advice_alignn_recommended`

Canonical week-2 summary bundle for the imported ALIGNN-recommended fine-tuning
namespace in `results_prof_advice_alignn_recommended/`.

- hyperparameters: `epochs=300`, `batch_size=64`, `learning_rate=0.001`
- coverage: `oxide` and `nitride`, `N=10/50/100/200/500/1000`, seeds `0..4`

This folder is regenerated from the imported namespace rather than copied verbatim
from Colab so its manifests use the canonical local layout.
